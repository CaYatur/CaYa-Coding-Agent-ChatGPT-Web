{
  "name": "caya-coding-agent",
  "version": "0.1.1",
  "private": true,
  "description": "A dependency-free bridge that lets a GPT Action operate on an explicitly selected local coding workspace.",
  "license": "MIT",
  "scripts": {
    "bridge": "node bridge/server.js",
    "agent": "node agent/index.js",
    "test": "node tests/e2e.test.js",
    "prepare-action-schema": "node tools/prepare-action-schema.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
