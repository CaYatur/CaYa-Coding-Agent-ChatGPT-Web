# Add inside the existing HTTPS VirtualHost.
# Public base URL: https://example.com/caya-agent

ProxyPreserveHost On
ProxyTimeout 60

ProxyPass        "/caya-agent/" "http://127.0.0.1:8787/"
ProxyPassReverse "/caya-agent/" "http://127.0.0.1:8787/"
